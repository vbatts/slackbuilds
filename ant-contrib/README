ant-contrib ( apache-ant task libraries)

The Ant-Contrib project is a collection of tasks (and at one point
maybe types and other tools) for Apache Ant.

You can pass the environment variable of ANT_HOME if it is different that /usr/share/ant

This package includes docs in /usr/doc/ant-contrib-$VERSION
